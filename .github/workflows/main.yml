KasirIhsan_FINAL.zip
      
    - name: Build APK Pake Buildozer
      uses: digreat/buildozer-action@v2
      with:
        python-version: 3.10
        workdir: .
        
    - name: Upload Hasil APK
      uses: actions/upload-artifact@v4
      with:
        name: APK-Kasir-Ihsan
        path: bin/*.apk
